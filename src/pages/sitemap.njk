---
permalink: /sitemap.xml
excludeFromSitemap: true
---
<!-- site.url: {{ site.url }} -->
<!-- collections.all.length: {{ collections.all.length }} -->

{% for page in collections.all %}
  <!-- page.url: {{ page.url }} -->
  <!-- page.data.title: {{ page.data.title }} -->
  <!-- page.date: {{ page.date }} -->
  <!-- page.data.excludeFromSitemap: {{ page.data.excludeFromSitemap }} -->
  <!-- page.filePathStem: {{ page.filePathStem }} -->
{% endfor %}

<?xml version="1.0" encoding="utf-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
    {%- for page in collections.all -%}
        {%- if not page.data.draft and not page.data.excludeFromSitemap and page.filePathStem != '/pages/sitemap' -%}
            <url>
                <loc>{{ site.url | url }}{{ page.url | url }}</loc>
                <lastmod>{{ page.date.toISOString() }}</lastmod>
                <changefreq>{{ page.data.changeFreq if page.data.changeFreq else "monthly" }}</changefreq>
            </url>
        {%- endif -%}
    {%- endfor -%}
</urlset>
